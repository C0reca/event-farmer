services:
  db:
    image: postgres:15-alpine
    container_name: teamevents_db
    environment:
      POSTGRES_USER: teamevents
      POSTGRES_PASSWORD: teamevents123
      POSTGRES_DB: teamevents
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U teamevents"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: teamevents_backend
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://teamevents:teamevents123@db:5432/teamevents
      - SECRET_KEY=your-secret-key-change-in-production-12345
      - ALGORITHM=HS256
      - ACCESS_TOKEN_EXPIRE_MINUTES=30
<<<<<<< HEAD
      - LLM_PROVIDER=openai
      - OPENAI_API_KEY=
      - LLM_MODEL=GPT-4o mini
=======
>>>>>>> parent of 720baa7 (c)
    stdin_open: true
    tty: true
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: teamevents_frontend
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:8000
    stdin_open: true
    tty: true
    depends_on:
      - backend

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: teamevents_pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@teamevents.com
      PGADMIN_DEFAULT_PASSWORD: admin123
    ports:
      - "5050:80"
    depends_on:
      - db

volumes:
  postgres_data:

